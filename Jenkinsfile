pipeline {

    agent any

    stages {

        stage('pipeline initialization') {
            steps {}
        }

        stage('code checkout') {
            steps{}
        }

        stage('prepare environment') {
            steps {}
        }
        
        stage('execute modules') {
            steps {}
        }

    }
}